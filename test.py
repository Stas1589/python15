# Запрашиваем данные у пользователя
# first_name = input("Введите ваше имя: ")
# last_name = input("Введите вашу фамилию: ")
# age = input("Введите ваш возраст: ")
#
# # Форматируем и выводим сообщение
# print(f"Hello, {last_name} {first_name}. Your age is: {age}")


# import math
#
# # Запрашиваем длины катетов
# a = float(input("Введите длину первого катета: "))
# b = float(input("Введите длину второго катета: "))
#
# # Вычисляем длину гипотенузы
# c = math.sqrt(a * a + b * b)
#
# # Выводим результат
# print(f"Длина гипотенузы: {c}")

# Запрашиваем длины сторон треугольника
# a = float(input("Введите длину первой стороны треугольника: "))
# b = float(input("Введите длину второй стороны треугольника: "))
# c = float(input("Введите длину третьей стороны треугольника: "))
#
# # Проверяем, является ли треугольник прямоугольным
# if round(c**2, 5) == round(a**2 + b**2, 5):
#     print("Треугольник является прямоугольным.")
# else:
#     print("Треугольник не является прямоугольным.")

# Запрашиваем список у пользователя
# user_list = input("Введите элементы списка через пробел: ").split()
#
# # Выводим элементы списка в обратном порядке
# print("Элементы списка в обратном порядке:", user_list[::-1])

# Исходные кортежи
# A = (1, 2, 3, 5, 8)
# B = (8, 2, 5)
#
# # Преобразуем кортеж A в список, чтобы добавить элементы
# A_list = list(A)
# A_list[2:2] = B
#
# # Преобразуем обратно в кортеж
# A_new = tuple(A_list)
#
# print(f"Новый кортеж: {A_new}")

from collections import Counter

# Запрашиваем список у пользователя
# user_list = input("Введите элементы списка через пробел: ").split()
# user_list = [int(x) for x in user_list]
#
# # Подсчитываем частоту каждого элемента
# counter = Counter(user_list)
# max_count = max(counter.values())
#
# # Находим элементы с максимальной частотой
# most_common = [num for num, count in counter.items() if count == max_count]
#
# print(f"Наиболее часто встречающееся число/числа: {most_common}")

# Запрашиваем количество секунд
# total_seconds = int(input("Введите количество секунд: "))

# Вычисляем дни, часы, минуты и секунды
# days = total_seconds // (24 * 3600)
# hours = (total_seconds % (24 * 3600)) // 3600
# minutes = (total_seconds % 3600) // 60
# seconds = total_seconds % 60
#
# # Выводим результат
# print(f"{total_seconds} секунд = {days}:{hours}:{minutes}:{seconds}")
